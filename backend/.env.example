# Database Configuration (PostgreSQL in production, SQLite fallback for local development)
DATABASE_URL=postgresql://fintrack_user:fintrack_password@localhost:5432/fintrack_db
# For SQLite local testing:
# DATABASE_URL=sqlite:///./fintrack.db

# JWT Authentication
JWT_SECRET_KEY=fintrack-super-secret-key-change-this-in-production-min-32-chars
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS Configuration
FRONTEND_URL=http://localhost:5173

# Environment (development / production)
ENVIRONMENT=development

# SMTP Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=irsathmajith113@gmail.com
SMTP_PASSWORD=hdqvdhtiisprdrkp
SMTP_FROM_EMAIL=irsathmajith113@gmail.com
SMTP_TLS=true

